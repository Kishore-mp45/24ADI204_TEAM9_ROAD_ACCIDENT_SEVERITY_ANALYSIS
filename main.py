from backend.main import app

# This file acts as the explicit Serverless entrypoint for Vercel deployment.
# Vercel's Python builder automatically scans the root directory for standard 
# entrypoint filenames (like main.py or app.py) to map the ASGI instance (FastAPI).
